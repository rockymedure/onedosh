# Copy to .env and add your key. The Dosh agent runs on Claude.
ANTHROPIC_API_KEY=sk-ant-...
# Optional overrides
DOSH_MODEL=claude-opus-4-6
PORT=8787

# Card Studio — generative card art (fal.ai). Without it, template drops still work.
FAL_KEY=
